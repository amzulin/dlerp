@{
    var list = Model.list as List<string>;
        var no=Model.no;
        var query=Model.query;
    }
<ul>
@if (list!=null&&list.Count>0)
{
    var index=0;
    foreach (var item in list)
    {
        <li id="li_@index" abc="@index"><a href="javascript:void(0);" onclick="sel_order('@item.Replace("_","")','@no')">@item.Replace("_","")</a>
            @if (@item.Contains("_"))
            {
                <i></i>
            } 
        </li>
    }
}
else
{
    <li>不存在委外工单</li>
}
</ul>

	@Html.Partial("~/Views/Shared/page_page.cshtml", new ViewDataDictionary()
       {           
           {"pageIndex",Model.pageIndex},{"totalCount",Model.totalCount},{"pageSize",Model.pageSize}
       })

